[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Volume for persistent state (config/*.json files)
# After first deploy, run: railway volume create -m /app/config
